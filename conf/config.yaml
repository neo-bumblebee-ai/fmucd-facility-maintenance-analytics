# Capstone — Facility Maintenance Analytics (FMUCD)
# Adjust paths and names for your Databricks workspace / Unity Catalog.

catalog: fmucd_capstone
schemas:
  bronze: bronze
  silver: silver
  gold: gold

tables:
  bronze_raw: bronze_fmucd_raw
  # Silver: Fact and Dimension tables
  fact_work_orders: fact_work_orders
  dim_building: dim_building
  dim_system: dim_system
  dim_subsystem: dim_subsystem
  dim_component: dim_component
  # Gold tables
  gold_wo_by_building: gold_wo_by_building
  gold_wo_by_system: gold_wo_by_system
  gold_ppm_upm_summary: gold_ppm_upm_summary
  gold_kpis: gold_kpis

paths:
  # FMUCD source: Downloaded from Kaggle to Databricks Volume
  volume_schema: fmucd_data
  volume_name: fmucd_data
  fmucd_csv: /Volumes/fmucd_capstone/fmucd_data/fmucd_data/Facility Management Unified Classification Database (FMUCD).csv
  
kaggle:
  # Update with your Kaggle dataset path (e.g., "username/dataset-name")
  dataset: ""  # e.g., "your-username/facility-management-unified-classification-database-fmucd"
  username: ""  # Your Kaggle username
  api_key: ""    # Your Kaggle API key (set via environment variable in notebook)
